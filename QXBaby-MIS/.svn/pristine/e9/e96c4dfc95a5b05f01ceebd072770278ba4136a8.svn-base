package com.onlineMIS.unit_testing;

import java.util.Date;
import java.util.List;

import org.hibernate.Criteria;
import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.Transaction;
import org.hibernate.cfg.Configuration;
import org.hibernate.criterion.Order;
import org.hibernate.criterion.Restrictions;

import com.onlineMIS.ORM.entity.chainS.chainMgmt.ChainStoreGroup;
import com.onlineMIS.ORM.entity.chainS.chainMgmt.ChainStoreGroupElement;
import com.onlineMIS.ORM.entity.chainS.inventoryFlow.ChainInOutStock;
import com.onlineMIS.ORM.entity.chainS.sales.ChainStoreSalesOrder;
import com.onlineMIS.common.Common_util;

public class testChainInOut {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
//	Configuration configuration = new Configuration().configure();
//		
//		SessionFactory sFactory = configuration.buildSessionFactory();
//		
//		Session session = sFactory.openSession();
//		Transaction transaction = session.beginTransaction();
//
//		Criteria criteria = session.createCriteria(ChainInOutStock.class);
//
//		criteria.add(Restrictions.eq("productBarcode.product.year.year_ID", 1));
//
//		
//		List<ChainInOutStock> chainStoreSalesOrders = criteria.list();
//		
//		System.out.println(chainStoreSalesOrders.size());
//		transaction.commit();
//		session.close();
		
		Integer quantityInOrderObj = new Integer("67");
		int quantityInOrder = quantityInOrderObj==null?0:quantityInOrderObj.intValue();
		System.out.print(quantityInOrder);
	}

}
